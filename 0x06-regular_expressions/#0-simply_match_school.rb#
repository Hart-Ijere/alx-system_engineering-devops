#!/usr/bin/env ruby
input_string = ARGV[0].to_s
pattern = /School/
matched_text = input_string.match(pattern)

if matched_text
  puts "Match found: #{matched_text[0]}"
else
  puts "No match found."
end



